
import { memo } from 'react';
import { useIsMobile } from '@/hooks/use-mobile';

const MobileBottomNav = () => {
  return null; // Effectively removes the mobile bottom navigation
};

export default memo(MobileBottomNav);
